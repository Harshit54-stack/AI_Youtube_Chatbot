# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — ROOT-LEVEL (Streamlit prototype + FastAPI backend shared)
#
# This file covers the Streamlit prototype (app.py / rag.py).
# The production FastAPI backend uses backend/requirements.txt instead.
#
# Install: pip install -r requirements.txt
#
# NOTE: youtube-transcript-api has been REMOVED — replaced by Supadata REST API.
#       Supadata avoids YouTube IP-blocking on cloud platforms (Render, Railway).
#       No local GPU or Groq account required. Use Google AI Studio API key.
# ─────────────────────────────────────────────────────────────────────────────

# ── Streamlit UI (prototype) ───────────────────────────────────────────────────
streamlit>=1.35.0

# ── LangChain ecosystem ───────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0

# ── Google Gemini LLM ─────────────────────────────────────────────────────────
# langchain-google-genai: LangChain-native Gemini integration (ChatGoogleGenerativeAI)
langchain-google-genai>=2.0.0

# ── Vector store ──────────────────────────────────────────────────────────────
faiss-cpu>=1.8.0

# ── HTTP client (Supadata API + langchain-google-genai internally) ─────────────
# Used to call the Supadata Transcript REST API to fetch YouTube transcripts.
httpx>=0.27.0

# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv>=1.0.0